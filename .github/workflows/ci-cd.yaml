name: Continuous Integration and Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Check out code 🔔
        uses: actions/checkout@v2

      - name: Setup .NET 5 🔧
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '5.0.x'
        
      - name: Setup msbuild ⚙
        uses: microsoft/setup-msbuild@v1.1        

      - name: Restore dependencies 🛠
        run: dotnet restore

      - name: Build App 🚀
        run: msbuild NoMoreYoyo/NoMoreYoyo.csproj -t:rebuild -verbosity:diag -property:Configuration=Release